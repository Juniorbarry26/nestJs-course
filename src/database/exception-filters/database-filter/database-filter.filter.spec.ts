import { DatabaseFilterFilter } from './database-filter.filter';

describe('DatabaseFilterFilter', () => {
  it('should be defined', () => {
    expect(new DatabaseFilterFilter()).toBeDefined();
  });
});
